<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChemAssist"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DemoActivity" />
        <activity android:name=".BalancerActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity android:name=".ReactionsActivity" />
        <activity android:name=".TableActivity" android:screenOrientation="landscape" />
        <activity android:name=".AlcaniActivity" />
        <activity android:name=".AlcheneActivity" />
        <activity android:name=".AlchineActivity" />
        <activity android:name=".AlcadieneActivity" />
        <activity android:name=".AreneActivity" />
        <activity android:name=".AlcooliActivity" />
        <activity android:name=".AldehideActivity" />
        <activity android:name=".AciziActivity" />
        <activity android:name=".EsteriActivity" />
        <activity android:name=".AmineActivity" />

    </application>
</manifest>
